[project]
name = "05-deployment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "scikit-learn==1.6.1",
    "fastapi",
    "uvicorn"
]

[dependency-groups]
dev = [
    "requests>=2.32.5",
]
